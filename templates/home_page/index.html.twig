{% extends 'base.html.twig' %}

{% block title %}Welcome to the homepage{% endblock %}

{% block body %}
    <div>
    </div>
    <div class="example-wrapper">
        <h1 id="hm1">Welcome to the Homepage! ✅</h1>
        <a href="{{ path('app_report_new') }}">Add new report!</a><br>
        {% if app.user is empty %}
            <a href="{{ path('app_login') }}">Log in!</a><br>
        {% endif %}
        {% if is_granted("ROLE_ADMIN") %}
            <a href="{{ path('app_report') }}">Reports list!</a><br>
            <a href="{{ path('app_register') }}">Add new user account!</a><br>
        {% endif %}
        {% if app.user is not empty %}
            {{ app.user.username }} <a href="{{ path('app_logout') }}">Logout</a>
        {% endif %}
    </div>
{% endblock %}